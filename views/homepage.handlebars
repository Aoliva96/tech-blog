{{#each posts as |post|}}
  <div class="post">
    <h2><a href="/post/{{post.id}}">{{post.title}}</a></h2>
    <p>Created by {{post.user.username}} on {{formatDate post.date_created}}</p>
  </div>
{{/each}}